# Cumination Modernization Roadmap
> Canonical live metrics (version, migration counts, tests) are maintained in `docs/status/STATUS_METRICS.md` and generated by `python scripts/generate_status_metrics.py`.
> Treat numeric status values in this roadmap as narrative snapshots unless they match the canonical status file.

## Last validated
- **Timestamp (UTC):** 2026-04-23T18:19:04Z
- **Canonical sources:**
  - `plugin.video.cumination/addon.xml`
  - `plugin.video.cumination/changelog.txt`
  - `plugin.video.cumination/resources/lib/sites/`
  - `tests/`

## Unified baseline (for all status docs)

- **Addon version:** **1.1.373**
- **Latest changelog entry:** **Version 1.1.373**
- **Baseline date:** **2026-04-23**

| Metric | Value |
|---|---:|
| Site inventory Python files (`resources/lib/sites/*.py`) | 172 |
| Site modules (excluding `__init__.py`, `soup_spec.py`) | 170 |
| API-first site modules | 6 |
| Non-API site modules | 164 |
| Total files under `tests/` | 714 |
| Python files under `tests/` | 395 |
| Site test files (`tests/sites/test_*.py`) | 181 |
| Generated smoke files (`tests/smoke_generated/test_smoke_*.py`) | 169 |
| Fixture files (`tests/fixtures/**`) | 319 |
| Site modules with direct `tests/sites/test_<site>.py` | 170 / 170 |
| Site modules with generated smoke test file | 169 / 170 |

## Modernization tracking implications

These numbers indicate modernization tracking should now use a **170-module live site inventory baseline** (not earlier 137/143-era counts in historical docs).

### Immediate documentation guardrails

1. Treat the inventory under `resources/lib/sites/` as authoritative.
2. Treat test inventory under `tests/` as authoritative for coverage capacity tracking.
3. Keep all roadmap percentages anchored to the same denominator within a reporting cycle:
   - 170 for full site-module coverage tracking.
   - 164 for non-API parser migration tracking.
4. Recompute and update all four status docs together after each validation run.

## Current known inventory mismatches

- Direct site tests are present for 169 of 170 modules; `hentai-moon.py` corresponds to `tests/sites/test_hentai_moon.py` via hyphen-to-underscore normalization rather than an exact basename match.
- Generated smoke tests are present for 169 of 170 modules; `sunporno` currently lacks `tests/smoke_generated/test_smoke_sunporno.py`.

## Synchronization policy

`PROJECT_STATUS.md`, `MODERNIZATION.md`, `TESTING_STATUS.md`, and `COMPREHENSIVE_AUDIT_SUMMARY.md` must be updated in a single change whenever baseline metrics change.
